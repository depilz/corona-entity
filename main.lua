------------------------------------------------------------------------------------------------------------------------
-- LIBRARIES --
------------------------------------------------------------------------------------------------------------------------
_G.screen   = require("Libs.screen")
_G.Class    = require("Libs.middleclass")

local idCounter = 0
function _G.generateID(object)
  idCounter = idCounter+1
  return object.."_"..idCounter
end

------------------------------------------------------------------------------------------------------------------------
-- PLUGINS --
------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------
-- SYSTEM EVENTS --
------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------
-- RUN GAME --
------------------------------------------------------------------------------------------------------------------------

local myEntity = require("entity"):new()

local circle = display.newCircle(myEntity.group, 0, 0, 10)
circle:setFillColor(.8,.2,.3)
myEntity:setPosition(screen.centerX-30, screen.centerY-30)
myEntity:move(60, 60, {
  time       = 4000,
  iterations = -1,
  transition = easing.inOutQuad
})
myEntity:scale(.01, {
  time       = 10000,
  iterations = -1,
  transition = easing.inQuad
})
myEntity:setVisibility(0, {
  time       = 2000,
  iterations = -1,
  transition = easing.inOutQuad
})
